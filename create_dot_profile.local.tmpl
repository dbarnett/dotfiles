export VCS_AUTHOR_EMAIL={{ if hasKey . "vcs_author_email" }}{{ .vcs_author_email }}{{ else }}{{ output "git" "config" "user.email" | trim }}{{ end }}

# Add any machine-specific local overrides here
# This file is a create_ file, so you can edit it freely without chezmoi overwriting
