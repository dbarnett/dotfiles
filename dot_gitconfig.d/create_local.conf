# Local git configuration overrides
# This file is managed by chezmoi but only created if it doesn't exist.
# Customize this file for machine-specific settings that shouldn't be synced.

# Example: URL rewrite for GitHub (machine-specific, not synced)
# [url "git@github.com:"]
#     insteadOf = https://github.com/
